---
layout: post
title:  "function 18.10.20"
---

CHAPTER 01 함수 기초  
=============

Lesson 01 함수 소개 
-------------

### 함수는 일종의 포장 기술입니다. 특정 기능을 하는 구문의 집함을 재사용하기 위해 포장하거나 그룹을 구분하기 위해 묶을 때 주로 사용하는 개념입니다.

### 01_함수는 이럴 때 사용해요.

#### 함수를 알아보기 전에 함수를 왜 배워야 하는지, 그리고 함수가 어떤 기능이 있는지 예를 이용해 알아보겠습니다.

#### 예제

> 여러분의 이름을 다음과 같이 5번 출력해 주세요.

> 출력 결과
> 1. 함수는 이럴때 사용.
> 2. 함수는 이럴때 사용.
> 3. 함수는 이럴때 사용.
> 4. 함수는 이럴때 사용.
> 5. 함수는 이럴때 사용.

[여러분의 이름을 다음과 같이 5번 출력해 주세요.](https://jsfiddle.net/5qjd81bu/1/)

#### 예제2

> "김수겸","신성민","윤홍란" 이름을 다섯번씩 출력해주세요

> 출력 결과
> 1. 김수겸
> 2. 김수겸
> 3. 김수겸
> 4. 김수겸
> 5. 김수겸

> 1. 신성민
> 2. 신성민
> 3. 신성민
> 4. 신성민
> 5. 신성민

> 1. 윤홍란
> 2. 윤홍란
> 3. 윤홍란
> 4. 윤홍란
> 5. 윤홍란

["김수겸","신성민","윤홍란" 이름을 다섯번씩 출력해주세요](https://jsfiddle.net/apL2ru3j/)

#### 함수를 활용한 경우 다음과 같이 크게 두가지의 장점이 있습니다.
#### 01. 코드 중복 제거 및 코드 재사용

![1](https://user-images.githubusercontent.com/42795906/47065814-39c79380-d21f-11e8-96b6-0c33e7b0a82c.gif)

<br /><br />

#### 02. 유지보수 

만약 출력 내용을 "김수겸"은 5번, 신성민은 3번으로 변경해야 하는 경우 반복문을 활용한 코드에서는 각각 수정해줘야 하지만, 함수의 매개변수를 이용하면 유지보수 하기 좋은 멋진 코드를 만들 수 있습니다.

#### 함수를 사용하는 이유?
함수를 활용하면 중복 코드 제거 및 코드를 재사용할 수 있을뿐 아니라 유지보수하기 좋은 코드를 만들수 있습니다.

<br /><br />

### 02_함수란?

#### 함수는 특정 기능을 하는 구문을 독립된 부품으로 만들어 재사용하고자 할 때 사용하는 문법입니다. 일종의 포장 방법이라고 할 수 있습니다.

![2](https://user-images.githubusercontent.com/42795906/47066502-51a01700-d221-11e8-9361-30e497143884.gif)

<br /><br />

### 03_함수 구조 3가지

#### 함수와 관련 된 내용

> 메개변수
> 지역변수와 전역변수
> 리턴값
> 함수 호출
> 함수 정의
> 함수 리터럴
> 함수 이름 규칙

#### 첫번쨰 : 일반적인 함수
<pre><code>function 함수이름 () {
  
 실행구문;
 
}</code></pre>

#### 두번쨰 : 매개변수가 있는 함수
<pre><code>function 함수이름 ([매개변수1 [, 매개변수2 [,...]]) {

  실행구문;

}</code></pre>

#### 세번쨰 : 리턴값이 있는 함수
<pre><code>function 함수이름 ([매개변수1 [, 매개변수2 [,...]]) {

  실행구문;
  return 실행결과;

}</code></pre>

<br /><br />

Lesson 02 가장 쉬운 함수 만들기 
-------------

### 가장 쉬운 함수 만드는 방법을 배웁니다. 함수 자체는 전혀 어렵지 않습니다. 정말 단순한 포장 기술일 뿐이라는 점을 알아주세요. 

<br />

### 01_사용법

#### 문법
<pre><code>function 함수이름 () {
  
  실행구문;
  ..... 
  
}</code></pre>
> 함수를 만드는 가장 일반적인 방법입니다. 함수 외부에서 함수 내부로 전달되는 데이터를 받는 매개변수도 없고 함수 내부의 정보를 함수밖으로 내보내는 리턴값도 없는 구조입니다. 그림으로 표현하면 다음과 같습니다.

![3](https://user-images.githubusercontent.com/42795906/47067866-a2b20a00-d225-11e8-9343-4b8c8d045a21.gif)

- 함수는 크게 함수 정의와 호출로 나눌 수 있습니다. 함수 정의는 일종의 설계도일 뿐 설계만 한다고 자동으로 동작하진 않습니다. 함수 내부에 포장돼 있는 실행 구문을 실행하고 싶을 때는 함수 호출을 해줘야 합니다.

#### function
변수 만들 때 사용하는 var 키워드와 마찬가지로 function은 함수를 만들 때 시용하는 키워드입니다. 풀어 설명하면 자바스크립트 엔진은 소스를 해석하는 과정에 function 키워드를 만나면 "음~ 여기는 합수를 만들라는 말이군" 으로 해석해 합수를 만들게 됩니다.

#### 함수 이름
함수 이름은 변수이름과 마찬가지로 유일해야 하며 만들려고 하는 함수의 기능을 함축한 의미가 담긴 이름으로 만들면 됩니다. 함수이름 작성법은
Lesson 06 에서 자세히 다룹니다.

#### { }
함수 영역을 나타냅니다.

#### 함수이름 ()
함수이름 + () 로 작성하면 함수가 동작합니다. 이 작업을 함수호출이라고 부릅니다.

#### 예제1

> 다음 내용을 hello()라는 함수로 만들어 유지보수하기 쉽게 만들어 주세요.

<pre><code>console.log("안녕하세요. 함수개쉬워여");
console.log("안녕하세요. 함수개쉬워여");
console.log("안녕하세요. 함수개쉬워여");</code></pre>

[다음 내용을 hello()라는 함수로 만들어 유지보수하기 쉽게 만들어 주세요.](https://jsfiddle.net/4toem7ay/)

#### 예제2

> 다음 내용을 printStar()라는 함수로 만들어 간결하게 만들어 주세요.

> 실행 결과
<pre><code>*
**
***
****
*****
*
**
***
****
*****</code></pre>

[다음 내용을 printStar()라는 함수로 만들어 간결하게 만들어 주세요.](https://jsfiddle.net/krf4tsz6/2/)

- 함수를 가장 쉽게 배우는 방법은 변수에 반복문까지 배우면서 만든 코드를 함수로 포장하는 연습을 익숙해질때까지 반복하는 것입니다.

<br /><br />

Lesson 03 지역변수 VS 전역변수
-------------

#### 매개변수가 있는 함수를 이해하기 위해서는 먼저 지역번수를 좀 더 자세히 알아야 합니다.

### 01_변수 종류

변수 | 설명 | 다루는 장
---- | ---- | ----
전역변수 | 전역에서 사용하는 데이터를 담는 변수이며 어디서든 접근해서 사용 가능합니다. 이제 막 스크립트를 시작한 초보라면 전역 변수를 가장 많이 만들게 됩니다.  | 1부 1
---- | ---- | ----
지역변수 | 특정 영역에서만 사용할 수 있는 변수입니다. 주로 함수 내부에 만들어지는 변수입니다 | 2부 1장
---- | ---- | ----
매개변수 | 함수 외부에서 함수 내부로 데이터를 전달하기 위한 용도로 사용하는 변수입니다. | 2부 1장
---- | ---- | ----
멤버변수 | 클래스 내부에 만들어지며 주로 객체에서 사용하는 정보를 담는 변수입니다. | 5부 1장

<br />

### 02_지역변수와 전역변수 구분하기

#### 지역변수와 전역변수를 만드는 방법은 동일합니다. 다른점이라면 만드는 영역이 다른 것이죠. 즉, 전역연역에 변수를 만들면 전역변수라 부르고 함수 내부 영역인 지역 영역에 변수를 만들면 지역변수라고 부릅니다.

<pre><code><script>

전역영역

function func1 () {
  
  지역영역
  
}

</script></code></pre>

#### 예제1 지역변수와 전역변수 구분하기

> 다음 예제를 실행하면 1,2,3,4,5에는 어떤 값이 출력될까요?

[1,2,3,4,5에는 어떤 값이 출력될까요?](https://jsfiddle.net/L6qnsxvo/)

- 지역변수의 접근 순위가 높기 때문.

<br />

### 03_지역변수와 전역변수 생명주기

#### 여기서 생명주기란 만들어지고 없어지는 시기를 나타냅니다. 지역변수와 전역변수는 이 생명주기가 다릅니다.

#### 예제2 지역변수와 전역변수 생명주기

<pre><code>function func1() {
	var name = "김수겸";
  console.log ("1. name = " + name);
}

func1();

console.log ("2. name = " + name);</code></pre>

[지역변수와 전역변수 생명주기](https://jsfiddle.net/6ohujxwd/)

- 전역변수의 생명주기는 자바스크립트가 실행되는 페이지가 브라우저에서 실행되고 있는 한 사라지지 않고 계속 남아있게 됩니다.

<br />

### 04_예제

#### 예제3 다음 예제를 실행하면 1,2,3,4에는 어떤값이 출력될까요?

[1,2,3,4에는 어떤값이 출력될까요?](https://jsfiddle.net/wkf4n18u/)

<br /><br />
Lesson 04 매개변수가 있는 함수 만들기
-------------

매개변수가 있는 함수를 만드는 방법을 배워보겠습니다. 매개변수가 있는 함수는 기본 함수 개념에 매개변수 부분만 추가된 구조이기 때문에, 기본 함수를 이해하고 있다면 쉽게 이해할 수 있을것입니다.

<br />

### 01_사용법

<pre><code>function 함수이름([매개변수1, 매개변수2, ....]) {
	
  실행구문
  
}</code></pre>

그림으로 표현하면 다음과 같습니다.

![1](https://user-images.githubusercontent.com/42795906/47136778-b5911100-d2ef-11e8-8cd8-f77795e71dcb.gif)

#### 01 매개변수는 이럴때 사용해요

<pre><code>function hello(value) {
  var name = value;
  console.log (name + "환영합니다.");
}

hello ("김수겸");
hello ("신성민");
hello ("윤홍란");</code></pre>

[매개변수는 이럴때 사용해요](https://jsfiddle.net/bzsnc1tp/)

- 이제 함수를 호출할때 마다 value 라는 구멍에 값을 넘겨 함수 내부에 있는 변수 name의 값을 변경할 수 있습니다.

#### 02 매개변수란?

매개변수는 변수의 일종이며 함수 외부에서 함수 내부로 데이터를 전달할 때 매개체 역할을 하는 변수입니다.

#### 03 매개변수 위치

매개변수 위치는 함수이름 안에 위치합니다.

#### 04 매개변수는 지역변수

매개변수는 지역변수이기도 합니다. 함수가 실행될 때 만들어지고 함수가 종료되면 자동으로 사라진다는 뜻입니다.

#### 05 매개변수 개수

매개변수 개수는 만들고 싶은 만큼 만들 수 있습니다. 다음처럼 매개변수 하나를 추가해 방문 횟수를 나타낼 수 있습니다.

<pre><code>function hello(value, count) {
  var name = value;
  console.log (name + "님 " + count + "번째 방문을 환영합니다.");
}

hello ("김수겸", 5);</code></pre>

[매개변수 하나를 추가해 방문 횟수를 나타낼 수 있습니다.](https://jsfiddle.net/4nd7vjzy/)


#### 06 매개변수에 데이터 전달하기

함수 매개변수에 데이터를 전달하는 방법은 함수를 호출할 때 데이터를 넣어 호출해 줍니다. 만약 매개변수가 두개면 함수 호출시 데이터를 두개를 넣어주는 거죠.

- 매개변수가 두개인데 값은 하나만 보내는 경우 두번째는 undefind가 저장됩니다.

<br />

### 02_예제

#### 예제1 매개변수로 받은 두 수를 더한 결과값을 출력하는 함수를 만들어 주세요.

[매개변수로 받은 두 수를 더한 결과값을 출력하는 함수를 만들어 주세요.](https://jsfiddle.net/w5qh3pjb/)

<br />

### 03_arguments란?

함수를 공부하게 되면 arguments를 만나게 되는데요. 초보자분들은 좀 어렵게 생각하는 것 중 하나입니다. 먼저 다음 예제를 풀어보세요.

#### 예제1 다음 코드가 실행되면 어떤 값이 출력될까요?

[다음 코드가 실행되면 어떤 값이 출력될까요?](https://jsfiddle.net/uzteo37n/)

- 함수 호출시 값을 넘겼지만 값을 받는 매개변수가 선언되어 있지 않기 때문에 아무런 값이 찍히지 않습니다. 자바스크립트는 name을 발견하면 우선 지역변수와 매개변수 중에서 name 을 찾게 됩니다. 만약 발견하지 못했다면 전역변수에서 찾아 사용합니다. 전역변수에도 없다면 name 이란 변수가 없는걸로 판단하고 에러를 발생시킵니다. 

그렇다면 매개변수를 선언하지 않고 매개변수 값에 접근하는 방법은 없을까요? 이런 경우를 위해 자바스크립트 함수는 arguments 라는 객체를 기본으로 제공합니다. arguments에는 모든 매개변수 값이 들어있습니다. 

> arguments란?<br />
> 매개변수의 모든 정보가 담겨있는 객체입니다.<br />
> 배열은 아니지만 배열처럼 사용<br />
> 매개변수의 개수는 argunemts의 length 프로퍼티를 이용하면 알 수 있습니다. 

[arguments란?](https://jsfiddle.net/rpkw7h0m/)

<br /><br />

Lesson 05 리턴값이 있는 함수 만들기 
-------------

이번 레슨에서는 리턴값이 있는 함수를 학습합니다. 기본 함수에 리턴값만 추가된 구조이기 때문에 기본함수를 이해하고 있다면 쉽게 이해할 수 있습니다. 

<pre><code>function 함수이름([매개변수1, 매개변수2,....]) {
	
  실행구문;
  
  [return 리턴값;]
  
});</code></pre>

#### 호출

var 변수 = 함수이름();

![untitled-2](https://user-images.githubusercontent.com/42795906/47140170-92b72a80-d2f8-11e8-9ada-281028bdad65.gif)

#### 리턴값이란?

함수 내부는 함수라는 철벽으로 포장돼 있기 때문에 한번 실행되면 함수 외부에서 접근할 수 없다고 했습니다. 이때 매개변수를 활용하면 함수 내부로 데이터를 전달할 수 있다고 배웠습니다. 리턴값은 매개변수와 반대되는 값입니다. 매개변수 값이 함수 외부에서 함수 내부로 들어오는 입력값이라면 리턴값은 함수 내부에서 처리한 결과값을 함수 외부로 전달하기 위해 사용하는 일종의 출력값입니다. 이때 사용하는 구문이 바로 return 명령어입니다.

![untitled-3](https://user-images.githubusercontent.com/42795906/47140642-baf35900-d2f9-11e8-8344-21bad8fc24bc.gif)

<br />

### 02_예제

#### 예제1 두 수를 매개변수로 받은 후 이 두 값을 더한 결과값을 리턴하는 함수 sum()을 만들어주세요.

[두 수를 매개변수로 받은 후 이 두 값을 더한 결과값을 리턴하는 함수 sum()을 만들어주세요.](https://jsfiddle.net/v2xdz831/)

- 그렇다면 리턴값이 있는 경우는 항상 변수로 받아야만 할까요? 아닙니다. 필요없다면 리턴값을 굳이 변수에 저장하지 않아도 됩니다. 과감히 버리세요.

<br />
### 03_return문의 또 다른 용도

return문은 함수 내부의 데이터를, 함수를 호출한 곳으로 전달할 때도 사용하지만 함수를 즉시 빠져 나오는 기능도 합니다. 이해를 돕기 위해 예를 하나 들어보겠습니다.

#### 예제2 무한 루프를 돌며 숫자를 입력받고, 입력받은 수의 합을 화면에 출력하는 기능을 sample() 라는 함수에 만들어주세요. 단, 입력값이 0이면 즉시 실행을 멈춰주세요.

[무한 루프를 돌며 숫자를 입력받고, 입력받은 수의 합을 화면에 출력하는 기능을 sample() 라는 함수에 만들어주세요. 단, 입력값이 0이면 즉시 실행을 멈춰주세요.](https://jsfiddle.net/be3vr7hx/)

- return의 또 다른 기능은 함수를 멈추고 즉시 탈출 할수 있는 기능을 가지고 있습니다. break와 비슷해 보이지만 완전히 다르게 동작합니다. break 문은 루프 탈출, return은 함수 탈출입니다.

<br /><br />
Lesson 06 함수이름 만들 때 주의사항
-------------

### 변수 이름 만드는것처럼 함수 이름 역시 지켜야할 규익이 있습니다. 함수 이름은 변수 이름 만드는것과 거의 동일합니다. 함수 이름 규칙에 대해 알아보죠.

#### 01_숫자로 시작하면 안되요

#### 02_대소문자 구분 : name와 Name은 완전히 다른 함수입니다. 

일반적으로 일반 함수의 경우 소문자로, 클래스를 만들 때는 대문자로 함수를 만듭니다. 

<pre><code>소문자로 시작하니 일반함수군. 호출해서 사용하면 되겠군

showUserName();</code></pre>

<pre><code>대문자로 시작하니 클래스군. 객체의 인스턴스를 생성해서 사용하면 되겠군

var user = new ShowUserName();</code></pre>

#### 03_낙타 표기법 : 여러 단어가 조합되는 경우 다음처럼 소문자와 대문자를 번갈아 넣어 표기

<pre><code>function showUserName(){

}</code></pre>

<br /><br />
Lesson 07 미션
-------------

#### 미션1 구구단 출력을 함수로 만들기

[2단에서 9단을 출력하는 print99() 함수 만들기](https://jsfiddle.net/x7cu6gL3/1/)

#### 미션2 사칙연산 계산기 만들기

[사칙연산 처리를 전문으로 하는 함수 만들기](https://jsfiddle.net/uxnopvwL/) <br />
[사칙연산 처리를 전문으로 하는 함수 만들기2](https://jsfiddle.net/6xt5p0wo/)

#### 미션3 심플 갤러리를 함수로 만들기

[심플 갤러리를 함수로 만들기](https://jsfiddle.net/3dcth8q0/15/)



